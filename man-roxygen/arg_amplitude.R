#' @param amplitude
#'   A numeric scalar specifying the carrier amplitude, in volts. For symmetric
#'   waveforms such as a sine wave, this is the voltage excursion from the
#'   configured offset to either peak. The output therefore ranges from
#'   \code{offset - amplitude} to \code{offset + amplitude}, corresponding to a
#'   peak-to-peak voltage of \code{2 * amplitude}.
