% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analog_out_configure.R
\name{SetCarrier}
\alias{SetCarrier}
\alias{SetCarrierFunction}
\alias{SetCarrierFrequency}
\alias{SetCarrierAmplitude}
\alias{SetCarrierOffset}
\alias{SetCarrierSymmetry}
\alias{SetCarrierPhase}
\title{Set carrier waveform parameters}
\usage{
SetCarrierFunction(device, channel, func)

SetCarrierFrequency(device, channel, frequency)

SetCarrierAmplitude(device, channel, amplitude)

SetCarrierOffset(device, channel, offset)

SetCarrierSymmetry(device, channel, symmetry)

SetCarrierPhase(device, channel, phase)

SetCarrier(
  device,
  channel,
  func = NULL,
  frequency = NULL,
  amplitude = NULL,
  offset = NULL,
  symmetry = NULL,
  phase = NULL
)
}
\arguments{
\item{device}{A \code{"dwf4r_device"} object returned by \code{OpenDevice()} that refers
to a device connection that is still open.}

\item{channel}{An integer scalar representing the zero-based Analog Out channel index.}

\item{func}{A string specifying the carrier waveform function. Available functions
depend on the selected device and can be queried with
\code{GetAnalogOutNodeFunctionTypes()}. The list of functions from the
manual:
\itemize{
  \item{\code{"dc"} - DC value set as offset;}
  \item{\code{"sine"} - sine waveform;}
  \item{\code{"square"} - square waveform (offset +/- amplitude);}
  \item{\code{"triangle"} - triangle waveform;}
  \item{\code{"ramp_up"} - waveform with a ramp-up voltage at the
    beginning;}
  \item{\code{"ramp_down"} - a waveform with a ramp-down voltage at the
    end;}
  \item{\code{"noise"} - noise waveform from random samples;}
  \item{\code{"pulse"} - pulse waveform (offset + amplitude);}
  \item{\code{"trapezium"} - trapezium;}
  \item{\code{"sine_power"} - sine with symmetry used as power function;}
  \item{\code{"sine_na"} - sine waveform for Network Analyzer, dynamic
    frequency adjustment without glitch}
  \item{\code{"dual_custom"} - double buffering of the waveform in the
    device for smooth switching.}
  \item{\code{"dual_pattern"} - double buffering of the waveform in the
    device for smooth switching.}
  \item{\code{"custom_pattern"} - waveform from custom repeated data with
    fixed sample rate;}
  \item{\code{"play_pattern"} - waveform in stream play style (it provides
    constant sample rate);}
  \item{\code{"custom"} - waveform from custom samples (optimizes for
    average requested frequency, sample output lengths may vary by one
    system frequency period);}
  \item{\code{"play"} - waveform in stream play style (optimizes for
    average requested frequency).}
}}

\item{frequency}{A numeric scalar specifying the frequency in Hz.}

\item{amplitude}{A numeric scalar specifying the carrier amplitude, in volts. For symmetric
waveforms such as a sine wave, this is the voltage excursion from the
configured offset to either peak. The output therefore ranges from
\code{offset - amplitude} to \code{offset + amplitude}, corresponding to a
peak-to-peak voltage of \code{2 * amplitude}.}

\item{offset}{A numeric scalar specifying the carrier offset, in volts. For symmetric
waveforms, the offset is the midpoint between the minimum and maximum
output voltages.}

\item{symmetry}{A numeric scalar specifying the symmetry in percent.}

\item{phase}{A numeric scalar specifying the carrier phase in degrees.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Set parameters of the carrier node for an Analog Out channel.

\code{SetCarrierFunction()} sets the waveform function of the carrier node.

\code{SetCarrierFrequency()} sets the frequency of the carrier node.

\code{SetCarrierAmplitude()} sets the amplitude of the carrier node.

\code{SetCarrierOffset()} sets the offset of the carrier node.

\code{SetCarrierSymmetry()} sets the symmetry of the carrier node.

\code{SetCarrierPhase()} sets the phase (in degrees) of the carrier node.

\code{SetCarrier()} sets one or more parameters of the carrier node for an
  Analog Out channel. Carrier parameters are updated sequentially. If an
  error occurs, parameters set before the error remain in the pending
  configuration but are not applied until \code{ApplyAnalogOutSettings()} or
  \code{StartAnalogOut()} is called.
}
\details{
These functions set carrier parameters only. Use
  \code{ApplyAnalogOutSettings()} to send updated settings to the device
  without changing the current channel state, or \code{StartAnalogOut()} to
  apply settings and start generation.

  The requested value may differ from the value actually configured by the
  device; use the corresponding \code{GetCarrier*()} function to retrieve the
  actual value.
}
\seealso{
See the vignette \emph{Basic Analog Out Functionality} for a complete
  Analog Out workflow.
}
