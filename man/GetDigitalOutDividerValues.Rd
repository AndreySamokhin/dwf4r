% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/digital_out_configure.R
\name{GetDigitalOutDividerValues}
\alias{GetDigitalOutDividerValues}
\alias{GetDigitalOutDivider}
\alias{GetDigitalOutInitialDivider}
\title{Get Digital Out divider values}
\usage{
GetDigitalOutDivider(device, channel, .validate_digital_out = TRUE)

GetDigitalOutInitialDivider(device, channel, .validate_digital_out = TRUE)
}
\arguments{
\item{device}{A \code{"dwf4r_device"} object returned by \code{OpenDevice()} that refers
to a device connection that is still open.}

\item{channel}{An integer scalar representing the zero-based Digital Out channel index.}

\item{.validate_digital_out}{A logical scalar indicating whether \code{AssertDigitalOut()} should be
called. This argument is intended for advanced use to avoid repeated
validation in controlled sequences of operations. Set to \code{FALSE} only
when the device and relevant Digital Out context, such as the channel and
supported configuration choices, have already been validated. The caller is
responsible for ensuring that the device remains open and the validated
context remains valid. Other argument validation and SDK error handling
remain active.}
}
\value{
An integer scalar containing the configured divider value, or a numeric
  whole-number scalar if the value exceeds R's integer range.
}
\description{
Retrieve the ordinary or initial divider configured for a selected Digital
  Out channel. The internal clock frequency and divider value determine the
  duration of one count. The initial divider is loaded when the instrument
  enters the Running state; after it expires, the ordinary divider is loaded
  on subsequent expirations.

\code{GetDigitalOutDivider()} returns the configured ordinary divider.

\code{GetDigitalOutInitialDivider()} returns the configured initial
  divider.
}
\details{
The functions return the values reported by the corresponding WaveForms
  SDK \code{*Get} functions.
}
\seealso{
See the vignette \emph{Basic Digital Out Functionality} for a complete
  Digital Out workflow.
}
