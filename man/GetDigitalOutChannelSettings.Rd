% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/digital_out_configure.R
\name{GetDigitalOutChannelSettings}
\alias{GetDigitalOutChannelSettings}
\alias{GetDigitalOutOutput}
\alias{GetDigitalOutFunction}
\alias{GetDigitalOutIdle}
\title{Get Digital Out channel settings}
\usage{
GetDigitalOutOutput(device, channel, .validate_digital_out = TRUE)

GetDigitalOutFunction(device, channel, .validate_digital_out = TRUE)

GetDigitalOutIdle(device, channel, .validate_digital_out = TRUE)
}
\arguments{
\item{device}{A \code{"dwf4r_device"} object returned by \code{OpenDevice()} that refers
to a device connection that is still open.}

\item{channel}{An integer scalar representing the zero-based Digital Out channel index.}

\item{.validate_digital_out}{A logical scalar indicating whether \code{AssertDigitalOut()} should be
called. This argument is intended for advanced use to avoid repeated
validation in controlled sequences of operations. Set to \code{FALSE} only
when the device and relevant Digital Out context, such as the channel and
supported configuration choices, have already been validated. The caller is
responsible for ensuring that the device remains open and the validated
context remains valid. Other argument validation and SDK error handling
remain active.}
}
\value{
A string identifying the configured Digital Out channel setting. Possible
  values for each setting can be queried with the corresponding capability
  function.
}
\description{
Retrieve the output mode, signal-generation function, or idle output mode
  currently configured for a selected Digital Out channel.

\code{GetDigitalOutOutput()} returns the configured output mode of a
  Digital Out channel.

\code{GetDigitalOutFunction()} returns the configured signal-generation
  function of a Digital Out channel.

\code{GetDigitalOutIdle()} returns the configured idle output mode of a
  Digital Out channel.
}
\details{
The WaveForms SDK and the public \pkg{dwf4r} API use different terms for
  selecting the type of generated signal. The SDK uses \emph{type}, whereas
  the R API uses \emph{function}. The latter is used primarily to maintain
  consistent naming between Analog Out and Digital Out functionality.
}
\seealso{
See the vignette \emph{Basic Digital Out Functionality} for a complete
  Digital Out workflow.
}
