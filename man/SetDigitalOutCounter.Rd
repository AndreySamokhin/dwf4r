% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/digital_out_configure.R
\name{SetDigitalOutCounter}
\alias{SetDigitalOutCounter}
\title{Set Digital Out low and high counters}
\usage{
SetDigitalOutCounter(
  device,
  channel,
  low_count,
  high_count,
  .validate_digital_out = TRUE
)
}
\arguments{
\item{device}{A \code{"dwf4r_device"} object returned by \code{OpenDevice()} that refers
to a device connection that is still open.}

\item{channel}{An integer scalar representing the zero-based Digital Out channel index.}

\item{low_count, high_count}{Non-negative whole-number scalars specifying the low and high counter
values respectively. The supported range can be queried with
\code{GetDigitalOutCounterRange()}.}

\item{.validate_digital_out}{A logical scalar indicating whether \code{AssertDigitalOut()} should be
called. This argument is intended for advanced use to avoid repeated
validation in controlled sequences of operations. Set to \code{FALSE} only
when the device and relevant Digital Out context, such as the channel and
supported configuration choices, have already been validated. The caller is
responsible for ensuring that the device remains open and the validated
context remains valid. Other argument validation and SDK error handling
remain active.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Set the low and high counter values of a selected Digital Out channel.
}
\details{
For pulse generation, the low and high counters determine how many counts
  the output remains low and high, respectively. The internal clock
  frequency and divider value determine the duration of one count.

  The WaveForms SDK specifies that a zero low or high counter value prevents
  the output level from toggling.

  This function updates the pending Digital Out configuration. With automatic
  configuration disabled by \pkg{dwf4r}, changing the counter values does not
  stop current Digital Out generation. Call \code{StartDigitalOut()} to start
  or restart the instrument with the updated configuration. To stop
  generation, call \code{StopDigitalOut()} explicitly.
}
\seealso{
See the vignette \emph{Basic Digital Out Functionality} for a complete
  Digital Out workflow.
}
